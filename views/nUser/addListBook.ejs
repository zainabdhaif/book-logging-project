<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&display=swap" rel="stylesheet">
    <title>add book to list</title>
</head>
<body>
    <h3> <%= lists.listType %></h3>
<fieldset>
    <legend>Add books to list [<%= lists.listType%>]</legend>
    <form action="/users/<%= user._id%>/nUser/list/<%=lists._id%>" method="POST">
        <label for="books">Choose a book:
            <select id="books" name="books" required>
                <option value="" select hidden>--select one--</option>
                <%  books.forEach((book) => {%>
                    <option value="<%=book._id%>"> <%= book.title%> </option>
                    <%    })%>
                </select>
            </label>
            <button type="submit">Add book to list</button>
                
         
            </form>
        </fieldset>
 
</body>
</html>